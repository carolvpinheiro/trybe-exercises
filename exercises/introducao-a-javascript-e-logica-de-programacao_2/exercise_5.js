let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];
//let higherNumber;

for (let index = 0; index < numbers.length; index += 1) {
    let eachNumber = numbers[index];

    for (let comparedIndex = 0; comparedIndex < numbers.length; index += 1) {
        let comparedNumber = numbers[comparedIndex];
    }

    if (eachNumber > comparedNumber) {
        console.log(comparedNumber);
    }
}